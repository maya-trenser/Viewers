name: NodeJS with Webpack

on:
  push:
    branches: [ "maya-trenser-action-1" ]
  pull_request:
    branches: [ "master" ]

jobs:
  buildCornerstone3D:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [16.x]

    steps:
        - name: Checkout
          uses: actions/checkout@v3
          with:
            repository: maya-trenser/cornerstone3D/maya-trenser-patch-1
            path: ./cornerstone3D

        - name: Use Node.js ${{ matrix.node-version }}
          uses: actions/setup-node@v3
          with:
            node-version: ${{ matrix.node-version }}

        - name: Build
          run: |
            cd ./cornerstone3D
            pwd
            yarn install
            yarn build:all

        - uses: actions/checkout@v3
          with:
            repository: maya-trenser/Viewers
            path: ./Viewers

        - name: Use Node.js ${{ matrix.node-version }}
          uses: actions/setup-node@v3
          with:
            node-version: ${{ matrix.node-version }}

        - name: Install
          run: |
            cd ./Viewers
            pwd
            yarn install

        - name: Copy
          run: |
            pwd
            cp -a ./cornerstone3D/packages/dicomImageLoader/dist ./Viewers/node_modules/@cornerstonejs/dicom-image-loader/dist
            cd ./Viewers
            yarn run build

  test:
    runs-on: ubuntu-latest
    needs:
      buildCornerstone3D
    steps:
    - name: echooo
      run: |
        echo "If you can read this, succeeded"
